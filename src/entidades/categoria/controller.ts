// import app from "@root/app";

import { findPackageJSON } from "node:module"
import { Categorias } from "./model.js"

// const baseURL = '/categoria';

// app.get(`${baseURL}/[id]`, (req, res)=> {
//     res.send(1);
// })

// app.get(`${baseURL}/[id]`, (req, res)=> {
//     res.send(1);
// })

// app.get(`${baseURL}/[id]`, (req, res)=> {
//     res.send(1);
// })

// app.get(`${baseURL}/[id]`, (req, res)=> {
//     const b =new Categorias(body)

//     try {
//         const erro = b.validar()
//     } catch (Jose) {
//         // açõpes
//         throw Jose;
//     }


//     if (!erro) {
//         // ação padrao
//     } else {
//         throw erro;
//     }
// })